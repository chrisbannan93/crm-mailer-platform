# Root project defaults (non-secret placeholders only)
PROJECT_NAME=crm-mailer-platform
VERTICAL=generic

# Local URLs (placeholders; actual services are defined in stack/.env and connector/.env)
TWENTY_URL=http://localhost:3000
LISTMONK_URL=http://localhost:9000
CONNECTOR_URL=http://localhost:4010
MAILPIT_URL=http://localhost:8025
