# Verticals

Vertical packs are isolated configuration modules loaded by `VERTICAL=<name>`.

## Rules
- `generic` is the default.
- Industry-specific logic stays in the relevant vertical folder.
- Core connector code only reads the pack contract (`vertical.json`).

## Current Packs
- `generic`
- `mortgage_au` (placeholder scaffold only)
